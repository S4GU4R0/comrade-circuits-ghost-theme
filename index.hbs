{{!-- Cyberpunk Homepage --}}

{{!-- Hero Section with Neon Effects --}}
{{#if @custom.show_hero_section}}
<section class="gh-hero">
    <div class="gh-hero-inner">
        <div class="gh-hero-content">
            <h1 class="gh-hero-title">
                {{#if @custom.hero_title}}
                    {{@custom.hero_title}}
                {{else}}
                    {{@site.title}}
                {{/if}}
            </h1>
            <p class="gh-hero-subtitle">
                {{#if @custom.hero_subtitle}}
                    {{@custom.hero_subtitle}}
                {{else}}
                    {{@site.description}}
                {{/if}}
            </p>
                    <div class="gh-hero-actions">
            <a href="#posts" class="gh-button gh-button-primary">EXPLORE POSTS</a>
            {{#if @custom.enable_search}}
                <a href="#" class="gh-button gh-button-secondary" data-portal="search">SEARCH</a>
            {{/if}}
        </div>
        </div>
        
        {{!-- Animated Background Elements --}}
        <div class="gh-hero-bg">
            <div class="matrix-rain"></div>
            <div class="circuit-lines"></div>
            <div class="neon-grid"></div>
        </div>
    </div>
</section>
{{/if}}

{{!-- Featured Posts Section --}}
<section id="posts" class="gh-posts">
    <div class="gh-posts-header">
        <h2 class="gh-posts-title">LATEST TRANSMISSIONS</h2>
        <div class="gh-posts-subtitle">Recent posts from the collective</div>
    </div>

    {{!-- Posts Grid with Neon Cards --}}
    <div class="gh-postfeed">
        {{#foreach posts}}
            <article class="gh-card">
                <a href="{{url}}" class="gh-card-link">
                    {{#if feature_image}}
                        <div class="gh-card-image">
                            <img src="{{feature_image}}" alt="{{title}}">
                            <div class="gh-card-overlay">
                                <div class="gh-card-readmore">READ MORE</div>
                            </div>
                        </div>
                    {{/if}}
                    
                    <div class="gh-card-content">
                        <header class="gh-card-header">
                            <h3 class="gh-card-title">{{title}}</h3>
                            {{#if excerpt}}
                                <p class="gh-card-excerpt">{{excerpt}}</p>
                            {{/if}}
                        </header>
                        
                        <footer class="gh-card-meta">
                            <div class="gh-card-author">
                                {{#if primary_author.profile_image}}
                                    <img src="{{primary_author.profile_image}}" alt="{{primary_author.name}}">
                                {{/if}}
                                <span>{{primary_author.name}}</span>
                            </div>
                            <time class="gh-card-date" datetime="{{date format="YYYY-MM-DD"}}">
                                {{date format="MMM DD, YYYY"}}
                            </time>
                            {{#if reading_time}}
                                <span class="gh-card-reading-time">{{reading_time}} min read</span>
                            {{/if}}
                        </footer>
                    </div>
                </a>
            </article>
        {{/foreach}}
    </div>

    {{!-- Pagination with Neon Effects --}}
    {{#if pagination}}
        <div class="gh-pagination">
            {{#if pagination.prev}}
                <a href="{{pagination.prev}}" class="gh-button gh-button-secondary">
                    <svg viewBox="0 0 24 24" fill="currentColor">
                        <path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"/>
                    </svg>
                    PREVIOUS
                </a>
            {{/if}}
            
            <div class="gh-pagination-info">
                Page {{pagination.page}} of {{pagination.pages}}
            </div>
            
            {{#if pagination.next}}
                <a href="{{pagination.next}}" class="gh-button gh-button-secondary">
                    NEXT
                    <svg viewBox="0 0 24 24" fill="currentColor">
                        <path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z"/>
                    </svg>
                </a>
            {{/if}}
        </div>
    {{/if}}
</section>

    {{!-- Services/Hosted Sites Section --}}
    {{#if @custom.show_hosted_sites}}
        {{#match @site.recommendations_enabled}}
        <section class="hosted-sites-section">
            <div class="container">
                <h2>{{@custom.hosted_sites_title}}</h2>
                
                <div class="sites-grid">
                    {{#match @custom.hosted_sites_limit "All"}}
                        {{#recommendations}}
                        <div class="site-card">
                            {{#if favicon}}
                                <img src="{{favicon}}" alt="{{title}}" class="site-favicon">
                            {{else}}
                                <div class="default-favicon">🌐</div>
                            {{/if}}
                            <div class="site-info">
                                <h3><a href="{{url}}" target="_blank" rel="noopener">{{title}}</a></h3>
                                {{#if description}}
                                    <p>{{description}}</p>
                                {{/if}}
                                <span class="site-url">{{readable_url}}</span>
                            </div>
                            <div class="visit-link">
                                <a href="{{url}}" target="_blank" rel="noopener">[VISIT]</a>
                            </div>
                        </div>
                        {{/recommendations}}
                    {{else}}
                        {{#recommendations limit=@custom.hosted_sites_limit}}
                        <div class="site-card">
                            {{#if favicon}}
                                <img src="{{favicon}}" alt="{{title}}" class="site-favicon">
                            {{else}}
                                <div class="default-favicon">🌐</div>
                            {{/if}}
                            <div class="site-info">
                                <h3><a href="{{url}}" target="_blank" rel="noopener">{{title}}</a></h3>
                                {{#if description}}
                                    <p>{{description}}</p>
                                {{/if}}
                                <span class="site-url">{{readable_url}}</span>
                            </div>
                            <div class="visit-link">
                                <a href="{{url}}" target="_blank" rel="noopener">[VISIT]</a>
                            </div>
                        </div>
                        {{/recommendations}}
                    {{/match}}
                </div>
                
                <div class="webring-style">
                    <p>← <a href="#" onclick="randomSite()">Random Site</a> | <a href="#" onclick="nextSite()">Next Site</a> →</p>
                </div>
            </div>
        </section>
        {{/match}}
    {{/if}}

    {{!-- Contact/Links Section --}}
    <section class="contact-section">
        <div class="container">
            <h2>{{#match @custom.heading_case "UPPERCASE"}}GET IN TOUCH{{/match}}{{#match @custom.heading_case "lowercase"}}get in touch{{/match}}{{#unless @custom.heading_case}}Get in Touch{{/unless}}</h2>
            
            <div class="contact-grid">
                <div class="contact-info">
                    <p>Want to join our collective? Got a project that needs hosting? Just want to talk tech?</p>
                    
                    <div class="contact-methods">
                        {{#if @custom.social_twitter}}
                            <p>📧 Twitter: <a href="https://twitter.com/{{@custom.social_twitter}}" target="_blank">@{{@custom.social_twitter}}</a></p>
                        {{/if}}
                        {{#if @custom.social_github}}
                            <p>🔧 GitHub: <a href="https://github.com/{{@custom.social_github}}" target="_blank">{{@custom.social_github}}</a></p>
                        {{/if}}
                        {{#if @custom.social_linkedin}}
                            <p>💼 LinkedIn: <a href="{{@custom.social_linkedin}}" target="_blank">Connect</a></p>
                        {{/if}}
                    </div>
                </div>
                
                <div class="ascii-art">
                    <pre>
    ╔══════════════════╗
    ║ TECH COLLECTIVE  ║
    ║ ════════════════ ║
    ║ [█] Building     ║
    ║ [█] Hosting      ║
    ║ [█] Teaching     ║
    ║ [█] Learning     ║
    ╚══════════════════╝
    </pre>
                </div>
            </div>
        </div>
    </section>

    {{!-- Footer CTA --}}
    <section class="cta-section">
        <div class="container">
            <div class="cta-box">
                <h3>Ready to Join?</h3>
                <p>{{@custom.post_cta_text}}</p>
                <div class="cta-buttons">
                    <a href="#/portal/signup" class="btn btn-neon" data-portal="signup">Join the Collective</a>
                    <a href="#/portal/" class="btn btn-outline" data-portal="">Member Login</a>
                </div>
            </div>
        </div>
    </section>
</main>

<script>
// Early 2000s style JavaScript
function randomSite() {
    const sites = document.querySelectorAll('.site-card a');
    if (sites.length > 0) {
        const randomIndex = Math.floor(Math.random() * sites.length);
        sites[randomIndex].click();
    }
}

function nextSite() {
    const sites = document.querySelectorAll('.site-card a');
    if (sites.length > 0) {
        const randomIndex = Math.floor(Math.random() * sites.length);
        sites[randomIndex].click();
    }
}

// Blinking cursor animation
setInterval(function() {
    const cursor = document.querySelector('.cursor');
    if (cursor) {
        cursor.style.opacity = cursor.style.opacity == '0' ? '1' : '0';
    }
}, 500);
</script>